{
  "version": "2.0.0",
  "fields": [
   {
        "field": "API_KEY_HEADER_NAME",
        "displayName": "Azure OpenAI API Key Header Name",
        "type": "String",
        "minLength": 4,
        "maxLength": 100,
        "required": true,
        "description": "Meraki API Key Header Name",
        "default": "api-key",
        "example": "api-key"
      },
      {
        "field": "API_KEY",
        "displayName": "Azure OpenAI API Key",
        "type": "String",
        "minLength": 4,
        "maxLength": 100,
        "required": true,
        "description": "Azure OpenAI API Key",
        "example": "7f0069e3c56f4361afe49a8caac72b93",
        "helpSection": "API Key"
      },
      {
        "field": "AUTH_TYPE",
        "displayName": "Azure OpenAI Auth Type",
        "type": "String",
        "minLength": 4,
        "maxLength": 60,
        "required": true,
        "description": "Azure OpenAI API Auth Type",
        "default": "API_KEY",
        "example": "API_KEY",
        "helpSection": "https://developer.cisco.com/meraki/api-latest/"
      },
    {
      "field": "SERVER_URL",
      "displayName": "Azure OpenAI Endpoint",
      "type": "String",
      "minLength": 3,
      "maxLength": 60,
      "required": true,
      "description": "Endpoint URL for Azure OpenAI Endpoint with the scheme. Ends with openai.azure.com",
      "default": "",
      "example": "https://<name>.openapi.azure.com/",
      "helpSection": "The Endpoint URL will end with openai.azure.com/"
    }
  ]
}
